@model IEnumerable<KalendarzWydarzenRodzinnych.Models.Powiadomienie>

@{
    ViewBag.Title = "List";
}

<h2>List</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DataPowiadomienia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.id_wydarzenie)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.id_uzytkownik)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tresc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.id_organizator)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DataPowiadomienia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.id_wydarzenie)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.id_uzytkownik)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tresc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.id_organizator)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
